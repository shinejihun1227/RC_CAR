# v4 요구사항

- 차량은 6 V, 100 RPM급 엔코더 포함 N20 메탈 기어드모터 4개를 사용한다.
- 각 모터는 TB6612FNG의 독립 채널 하나에 연결한다.
- 각 모터는 엔코더 A 채널 1개로 속도를 측정한다. 방향은 모터 명령에서 알 수 있으므로 첫 실습에서는 B 채널을 연결하지 않는다.
- 4개의 모터 속도를 각각 PID로 제어한다.
- v1의 거리 안전 정지와 v2의 통신 끊김 자동 정지는 최우선으로 동작한다.
