# RC_CAR 작업·게시 기준

사용자가 지정한 강의 방식은 Arduino IDE에서 C언어 문법 중심으로 실습하는 것이다. GitHub에는 강의에서 직접 사용하는 Arduino 코드, 배선, 부품 설명·부품표, 설계 파일과 수업 안내를 게시한다.

- 공개 폴더는 `v0`~`v4`, `course`, `parts`, `hardware`이다.
- 실습은 ESP32와 Arduino IDE·시리얼 모니터로 진행한다. 학생의 제어 로직은 C 스타일 변수·조건문·함수·배열·구조체로 작성한다. `.ino`와 Arduino/센서 라이브러리는 C++로 컴파일된다는 사실을 정확히 설명한다.
- PC용 Python 시리얼 실습은 로컬 `_local/archive/python_serial_labs`에 보관한다. Fusion 360용 Python은 설계에 직접 쓰는 도구이므로 `hardware`에 유지한다.
- PPT/PPTX, 한글/HWPX, Word, 강의 기획·행정 문서, 제작 중간 파일, 이미지 미리보기, 임시 폴더, 의존성, 검증 로그, 이전 설계안은 로컬 `_local`에 보관하고 push하지 않는다.
- 기존 로컬 자료를 지우지 말고 `_local`로 보관한다. 바탕화면 등 다른 사본은 일괄 덮어쓰거나 삭제하지 않는다.
- 새 자료는 어떤 버전의 어떤 실습에서 쓰는지 해당 README에 연결한다. 라이브러리 전체 소스 대신 설치 안내를 제공한다.
- 각 버전 README에는 학습 목표, 코딩 개념, 부품·하드웨어, 배선 링크, 실행 순서, 완료 기준, 다음 단계를 설명한다.
- 코드의 실제 구현과 문서 설명을 맞춘다. 실물 시험을 하지 않았다면 완료·검증됨으로 표현하지 않는다. 실제 MAC/PPR·부품 치수를 추측해서 채우지 않는다.
- Arduino 스케치 파일과 폴더 이름을 일치시키고 `v0`~`v4`의 실습 경로를 유지한다.
- push 전에 변경 목록과 문서 링크, 필요한 코드 검증을 확인한다. 명시한 공개 경로만 stage하며 `git add -f`로 제외 규칙을 우회하지 않는다.
- 로컬 백업, 개인 경로, 장비별 비밀값, 강의자료 원본이 staged 목록에 없는지 확인한다.
- GitHub의 기존 파일 교체는 일반 커밋으로 현재 트리를 갱신한다. 사용자가 별도로 요청하지 않으면 강제 push, Git 이력 삭제, 저장소 삭제를 하지 않는다.

이 게시 범위는 이후 작업에도 적용한다. 실제 push는 그 작업에 대한 사용자의 요청 범위에서 수행한다.
