# v0 검증 순서

1. 배터리를 연결하지 않은 상태에서 ESP32와 TB6612FNG GND 공통을 확인한다.
2. `01_motor_test`를 업로드해 양쪽 모터의 전진·후진을 확인한다.
3. 방향이 반대면 모터 단자를 바꾸거나 코드의 방향을 반전한다.
4. `02_basic_drive`에서 `f`, `b`, `l`, `r`, `s` 명령을 시험한다.
5. 모터가 동작할 때 ESP32가 재부팅되면 5 V 전원과 470 uF 커패시터 배치를 점검한다.
